{
  "name": "parser-display",
  "version": "1.0.0",
  "description": "Display the json result of the parser python script",
  "scripts": {
    "build": "npx parcel build src/index.html --public-url ./",
    "serve": "npx parcel serve src/index.html",
    "clean": "rm -rf .cache; rm -rf dist; rm -rf coverage .nyc_output",
    "clean-all": "npm run clean; rm -rf node_modules package-lock.json",
    "doc": "npx esdoc"
  },
  "author": "Louis Hermier",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
